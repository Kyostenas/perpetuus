import { ElementoOcultableDirective } from './elemento-ocultable.directive';

describe('ElementoOcultableDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementoOcultableDirective();
    expect(directive).toBeTruthy();
  });
});
