import { BootstrapHideAutoDirective } from './bootstrap-hide-auto.directive';

describe('BootstrapHideAutoDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapHideAutoDirective();
    expect(directive).toBeTruthy();
  });
});
