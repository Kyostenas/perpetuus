import { BootstrapShowAutoDirective } from './bootstrap-show-auto.directive';

describe('BootstrapShowAutoDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapShowAutoDirective();
    expect(directive).toBeTruthy();
  });
});
