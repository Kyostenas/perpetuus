import { SwitchBootstrapShowDirective } from './switch-bootstrap-show.directive';

describe('SwitchBootstrapShowDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchBootstrapShowDirective();
    expect(directive).toBeTruthy();
  });
});
