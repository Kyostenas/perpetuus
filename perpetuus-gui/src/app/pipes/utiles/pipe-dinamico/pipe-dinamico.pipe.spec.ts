import { PipeDinamicoPipe } from './pipe-dinamico.pipe';

describe('PipeDinamicoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDinamicoPipe();
    expect(pipe).toBeTruthy();
  });
});
